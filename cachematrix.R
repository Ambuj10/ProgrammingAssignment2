## cache the inverse of a matrix
## functions do

## Takes matrix and returns a list with matrix and catched result.

makeCacheMatrix <- function(x = matrix()) {
  i <- NULL
  set <- function(y) {
    x <<- y
    i <<- NULL
  }
  get <- function() x
  setinverse <- function(inverse) i <<- inverse
  getinverse <- function() i
  list(set = set, get = get,
       setinverse = setinverse,
       getinverse = getinverse)
}


## Checks if resulted is cached previously then returns cached data otherwise compute and catches a result for future.ssss

cacheSolve <- function(x, ...) {
        i <- x$getinverse()
  if(!is.null(i)) {
    message("getting cached data")
    return(i)
  }
  data <- x$get()
  i <- solve(data, ...)
  x$setinverse(i)
i
}
